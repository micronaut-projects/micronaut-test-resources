Pulsar support will automatically start an https://pulsar.apache.org/[Apache Pulsar container] and provide the value of the `pulsar.service-url` property.

The default image (`{default-image-pulsar}`) can be overridden by setting the `test-resources.containers.pulsar.image-name` property.
