plugins {
    id 'io.micronaut.build.internal.test-resources-module'
}

description = """
Shared binary codec support for the test resources client/server transport.
"""

dependencies {
    implementation(mn.micronaut.http)
}
