jpa:
  default:
    reactive: true
    entity-scan:
      packages: io.micronaut.testresources.hibernate.reactive.core
    properties:
      hibernate:
        connection:
          db-type: mssql
        hbm2ddl:
          auto: update
