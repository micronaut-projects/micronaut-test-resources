datasources:
  default:
    db-type: mysql
    schema-generate: CREATE_DROP
    dialect: MYSQL
jpa:
  default:
    reactive: true
    entity-scan:
      packages: io.micronaut.testresources.hibernate.reactive.core
    properties:
      hibernate:
        hbm2ddl:
          auto: update
