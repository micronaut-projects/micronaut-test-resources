datasources:
  default:
    db-type: oracle
    schema-generate: CREATE_DROP
    dialect: ORACLE
jpa:
  default:
    reactive: true
    entity-scan:
      packages: io.micronaut.testresources.hibernate.reactive.core
    properties:
      hibernate:
        hbm2ddl:
          auto: update
