datasources:
  default:
    db-type: postgres
    schema-generate: CREATE_DROP
    dialect: POSTGRES
jpa:
  default:
    reactive: true
    entity-scan:
      packages: io.micronaut.testresources.hibernate.reactive.core
    properties:
      hibernate:
        hbm2ddl:
          auto: update
