datasources:
  default:
    driverClassName: org.mariadb.jdbc.Driver
    schema-generate: CREATE_DROP
    dialect: MYSQL
r2dbc:
  datasources:
    default:
      db-type: mariadb
      schema-generate: CREATE_DROP
      dialect: MYSQL
