datasources:
  default:
    driverClassName: com.mysql.cj.jdbc.Driver
    schema-generate: CREATE_DROP
    dialect: MYSQL
r2dbc:
  datasources:
    default:
      schema-generate: CREATE_DROP
      dialect: MYSQL
