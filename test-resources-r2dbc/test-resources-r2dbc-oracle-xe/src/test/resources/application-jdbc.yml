datasources:
  default:
    db-type: oracle-xe
    driverClassName: oracle.jdbc.OracleDriver
    schema-generate: CREATE_DROP
    dialect: ORACLE
r2dbc:
  datasources:
    default:
      schema-generate: CREATE_DROP
      dialect: ORACLE
